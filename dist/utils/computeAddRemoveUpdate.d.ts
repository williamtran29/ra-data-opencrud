export declare const computeFieldsToAdd: (oldIds: string[], newIds: string[]) => {
    id: string;
}[];
export declare const computeFieldsToRemove: (oldIds: string[], newIds: string[]) => {
    id: string;
}[];
export declare const computeFieldsToUpdate: (oldIds: string[], newIds: string[]) => {
    id: string;
}[];
export declare const computeFieldsToAddRemoveUpdate: (oldIds: string[], newIds: string[]) => {
    fieldsToAdd: {
        id: string;
    }[];
    fieldsToRemove: {
        id: string;
    }[];
    fieldsToUpdate: {
        id: string;
    }[];
};
//# sourceMappingURL=computeAddRemoveUpdate.d.ts.map